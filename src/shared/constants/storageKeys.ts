export const STORAGE_KEYS = {
  accessToken: 'myceo-access-token',
  authSessionSnapshot: 'myceo-auth-session-snapshot',
  complianceConsent: 'myceo-compliance-consent',
  persistRoot: 'myceo-root',
  refreshToken: 'myceo-refresh-token',
} as const;
